# React + TypeScript + Vite + Tailwind Demo

This demo implements a Todo list in React, Typescript, Vite, and Tailwind. It also implements a Github workflow to automatically build and deploy the webapp.